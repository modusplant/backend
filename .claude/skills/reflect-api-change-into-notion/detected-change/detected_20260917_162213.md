# Detected Code Change — 20260917_162213

Run ID: 20260917_162213
Detected At: 2026-09-17T16:22:13+09:00
Target-Class Resolution: Manual override (user-specified — bypassed `# Target Classes` cascade and
`# Watched API Surface` filter; user asked to document a single named endpoint directly)
Target Classes: MemberRestController (endpoint: `overrideMemberProfile_v4`, `PUT /api/v4/members/profile`)

## kr.modusplant.domains.member.framework.inbound.web.rest.MemberRestController

- Change Kind: Pre-existing gap (not a code change) — endpoint already exists in code, has no Notion page yet
- Layer: RestController
- Domain/Package: domains.member.framework.inbound.web.rest
- Facts:
  - Method `overrideMemberProfile_v4`, route `@PutMapping(value = "/v4/members/profile")`, class-level `@RequestMapping("/api")` → full path `PUT /api/v4/members/profile`
  - `@PreAuthorize("isAuthenticated()")`; no `@Hidden`
  - Three method parameters bound as implicit `@RequestParam`-style query parameters (no `@RequestBody`, no `consumes` override): `fileKey` (`required = false`), `introduction` (`required = false`), `nickname` (`@NotBlank` + `@Pattern(regexp = REGEX_NICKNAME)`, effectively required)
  - `memberId` comes from `@AuthenticationPrincipal(expression = "uuid")`, not a client-supplied parameter
  - Delegates to `memberController.overrideProfile(new MemberProfileOverrideRecord_V4(memberId, introduction, fileKey, nickname))`
  - Returns `200 OK`, `Cache-Control: no-store, must-revalidate, private`, body `DataResponse.ok(MemberProfileResponseWithImageUrl)`
  - Only difference from the sibling `overrideMemberProfile_v3` (same file, `PUT /api/v3/members/profile`): v3 returns `MemberProfileResponseWithImagePath`, v4 returns `MemberProfileResponseWithImageUrl` — identical request shape and identical validation/failure paths otherwise
- Relevant Code Excerpt:
  ```java
  @PutMapping(value = "/v4/members/profile")
  @PreAuthorize("isAuthenticated()")
  public ResponseEntity<DataResponse<MemberProfileResponseWithImageUrl>> overrideMemberProfile_v4(
          @RequestParam(required = false) String fileKey,
          @RequestParam(required = false) String introduction,
          @NotBlank(message = "회원 닉네임이 비어 있습니다. ")
          @Pattern(regexp = REGEX_NICKNAME, message = "회원 닉네임 서식이 올바르지 않습니다. ")
          String nickname,
          @AuthenticationPrincipal(expression = "uuid") UUID memberId) throws IOException {
      return ResponseEntity.status(HttpStatus.OK)
              .cacheControl(CacheControl.noStore().mustRevalidate().cachePrivate())
              .body(DataResponse.ok(
                      memberController.overrideProfile(
                              new MemberProfileOverrideRecord_V4(memberId, introduction, fileKey, nickname))));
  }
  ```

## kr.modusplant.domains.member.adapter.controller.MemberController

- Change Kind: Pre-existing gap (not a code change)
- Layer: Controller
- Domain/Package: domains.member.adapter.controller
- Facts:
  - `overrideProfile(MemberProfileOverrideRecord_V4)` calls `validateBeforeOverrideProfile`, which: (1) `memberValidationHelper.validateIfMemberExists(memberId)` — throws on missing member; (2) `swearService.isSwearContained(nickname.getValue())` → throws `SwearContainedException` if profane; (3) looks up an existing member by nickname via `memberRepository.getByNickname`, and if present and not the same member, throws `ExistsEntityException(KernelErrorCode.EXISTS_NICKNAME, "nickname")`; (4) `memberImageIOHelper.validateIfImageExistsInStorage(memberProfileImage.getMemberProfileImagePath())` — throws if the S3 object doesn't exist
  - Builds `Nickname` (kernel VO, may throw `EMPTY_NICKNAME`/`INVALID_NICKNAME_FORMAT` — not reachable via this REST endpoint since `@NotBlank`/`@Pattern` on the controller parameter already rejects blank/malformed input with the same regex before this code runs)
  - Builds `MemberProfileImagePath.create(record.fileKey())` — throws `MemberErrorCode.INVALID_MEMBER_PROFILE_IMAGE_PATH` on a malformed path string
  - Filters introduction via `swearService.filterSwear`, then builds `MemberProfileIntroduction.create(...)` — throws `MemberErrorCode.MEMBER_PROFILE_INTRODUCTION_OVER_LENGTH` if length > 60 after filtering
  - Deletes the old profile image from S3 via `memberImageIOHelper.deleteImage`, then persists the new `MemberProfile` via `memberProfileRepository.update(memberProfile, true)` — the `true` flag selects the S3-URL response mapping (vs. `false`/raw-path used by v3)
- Relevant Code Excerpt:
  ```java
  public MemberProfileResponseWithImageUrl overrideProfile(MemberProfileOverrideRecord_V4 record) throws IOException {
      MemberId memberId = MemberId.fromUuid(record.id());
      Nickname memberNickname = Nickname.create(record.nickname());
      MemberProfileImage memberProfileImage = MemberProfileImage.create(
              MemberProfileImagePath.create(record.fileKey()),
              MemberProfileImageBytes.create(null));
      validateBeforeOverrideProfile(memberId, memberNickname, memberProfileImage);
      MemberProfile memberProfile = memberProfileRepository.getByIdWithoutImageBytes(memberId);
      memberImageIOHelper.deleteImage(memberProfile.getMemberProfileImage());
      MemberProfileIntroduction memberProfileIntroduction =
              MemberProfileIntroduction.create(swearService.filterSwear(record.introduction()));
      memberProfile = MemberProfile.create(memberId, memberProfileImage, memberProfileIntroduction, memberNickname);
      return (MemberProfileResponseWithImageUrl)
              memberProfileMapper.toMemberProfileResponse(memberProfileRepository.update(memberProfile, true), true);
  }
  ```

## kr.modusplant.domains.member.usecase.response.MemberProfileResponseWithImageUrl

- Change Kind: Pre-existing gap (not a code change)
- Layer: Response
- Domain/Package: domains.member.usecase.response
- Facts:
  - `record MemberProfileResponseWithImageUrl(UUID id, String imageUrl, String introduction, String nickname) implements MemberProfileResponse`
  - Constructed by `MemberProfileMapperImpl.toMemberProfileResponse(memberProfile, true)`: `imageUrl` is `null` when there is no image, otherwise an S3-generated URL via `amazonS3Service.generateS3SrcUrl(imagePath)`

## kr.modusplant.domains.member.domain.exception.enums.MemberErrorCode

- Change Kind: Pre-existing gap (not a code change)
- Layer: ErrorCode
- Domain/Package: domains.member.domain.exception.enums
- Facts:
  - `NOT_FOUND_MEMBER_ID(HttpStatus.NOT_FOUND.value(), "not_found_member_id", "회원 아이디를 찾을 수 없습니다. ")` — line 63
  - `INVALID_MEMBER_PROFILE_IMAGE_PATH(400, "invalid_member_profile_image_path", "회원 프로필 이미지 경로의 서식이 올바르지 않습니다. ")`
  - `MEMBER_PROFILE_INTRODUCTION_OVER_LENGTH(400, "member_profile_introduction_over_length", "회원 프로필 소개가 허용되는 길이를 초과하였습니다. ")`

## kr.modusplant.shared.kernel.enums.KernelErrorCode

- Change Kind: Pre-existing gap (not a code change)
- Layer: ErrorCode
- Domain/Package: shared.kernel.enums
- Facts:
  - `EXISTS_NICKNAME(HttpStatus.BAD_REQUEST.value(), "exists_nickname", "닉네임이 이미 존재합니다")` — line 24; thrown from `MemberController.validateBeforeOverrideProfile` as `ExistsEntityException(KernelErrorCode.EXISTS_NICKNAME, "nickname")`
  - `EMPTY_NICKNAME`/`INVALID_NICKNAME_FORMAT` exist but are not reachable through this endpoint (superseded by REST-layer `@NotBlank`/`@Pattern` on the same `nickname` parameter)

## kr.modusplant.infrastructure.swear.exception.enums.SwearErrorCode

- Change Kind: Pre-existing gap (not a code change)
- Layer: ErrorCode
- Domain/Package: infrastructure.swear.exception.enums
- Facts:
  - `SWEAR_CONTAINED(HttpStatus.BAD_REQUEST.value(), "swear_contained", "값에 비속어가 포함되어 있습니다. ")` — thrown as `SwearContainedException` from `MemberController.validateBeforeOverrideProfile` when the nickname contains profanity

## kr.modusplant.shared.framework.aws.exception.enums.AWSErrorCode

- Change Kind: Pre-existing gap (not a code change)
- Layer: ErrorCode
- Domain/Package: shared.framework.aws.exception.enums
- Facts:
  - `NOT_FOUND_FILE_KEY_ON_S3(HttpStatus.NOT_FOUND.value(), "not_found_file_key_on_s3", "Amazon S3에서 파일 키를 찾을 수 없습니다. ")` — thrown by `memberImageIOHelper.validateIfImageExistsInStorage` when the given `fileKey` isn't present in storage

## kr.modusplant.infrastructure.security.config.SecurityConfig

- Change Kind: Pre-existing gap (not a code change)
- Layer: SecurityConfig
- Domain/Package: infrastructure.security.config
- Facts:
  - `PUBLIC_ENDPOINTS` has no `HttpMethod.PUT` key at all, and no pattern matches `/api/v4/members/profile` under any method
  - Default rule `auth.anyRequest().authenticated()` applies (line 203) → authentication required
  - `/api/admin/**`-only rule (`hasAuthority("ADMIN")`, line 200) does not match this path
