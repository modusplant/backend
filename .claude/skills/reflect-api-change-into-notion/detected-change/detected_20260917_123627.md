# Detected Code Change — 20260917_123627

Run ID: 20260917_123627
Detected At: 2026-09-17T12:36:27+09:00
Target-Class Resolution: Pre-existing gap (not a code change) — hand-authored E2E test input, skipping the Target Classes detection cascade per explicit instruction. Facts below are transcribed directly from the current state of each listed file, not from a git diff.
Target Classes: MemberRestController, MemberAdminRestController, CommentRestController, IdentityRestController, NormalIdentityRestController

## kr.modusplant.domains.member.framework.inbound.web.rest.MemberRestController

- Change Kind: Pre-existing gap (not a code change)
- Layer: RestController
- Domain/Package: domains.member
- Facts:
  - `checkExistedMemberNickname` (`GET /api/v1/members/check/nickname/{nickname}`): path variable `nickname` — `@PathVariable(required = false) @NotBlank @Pattern(regexp = REGEX_NICKNAME)`, type `String`.
  - `likeCommunicationPost` (`PUT /api/v1/members/like/communication/post/{postUlid}`): path variable `postUlid` — `@PathVariable(required = false) @NotBlank`, type `String` (ULID format).
  - `unlikeCommunicationPost` (`DELETE /api/v1/members/like/communication/post/{postUlid}`): path variable `postUlid` — same shape as above.
  - `bookmarkCommunicationPost` (`PUT /api/v1/members/bookmark/communication/post/{postUlid}`): path variable `postUlid` — same shape as above.
  - `cancelCommunicationPostBookmark` (`DELETE /api/v1/members/bookmark/communication/post/{postUlid}`): path variable `postUlid` — same shape as above.
  - `likeCommunicationComment` (`PUT /api/v1/members/like/communication/post/{postUlid}/path/{path}`): two path variables — `postUlid` (`String`, ULID) and `path` (`String`, materialized path, e.g. `1.0.4`), both `@PathVariable(required = false) @NotBlank`.
  - `unlikeCommunicationComment` (`DELETE /api/v1/members/like/communication/post/{postUlid}/path/{path}`): same two path variables as above.
  - `reportPostAbuse` (`POST /api/v1/report/abuse/post/{postUlid}`): path variable `postUlid` — `@PathVariable @NotBlank`, type `String` (ULID).
  - `reportCommentAbuse` (`POST /api/v1/report/abuse/post/{postUlid}/path/{path}`): two path variables — `postUlid` (`String`, ULID, `@PathVariable @NotBlank`) and `path` (`String`, materialized path, `@PathVariable(required = false) @NotBlank`).
  - None of these 9 methods' Notion pages currently document these path variables as a `### 경로 변수` table — they exist only in the Notion database's `URI` property.
- Relevant Code Excerpt:
  ```java
  @PathVariable(required = false)
  @NotBlank(message = "게시글 식별자가 비어 있습니다.")
  String postUlid
  ```

## kr.modusplant.domains.member.framework.inbound.web.rest.MemberAdminRestController

- Change Kind: Pre-existing gap (not a code change)
- Layer: RestController
- Domain/Package: domains.member (admin)
- Facts:
  - `removeProposalOrBugReport` (`DELETE /api/admin/v1/report/proposal-or-bug/{reportUlid}`): path variable `reportUlid` — `@PathVariable @NotBlank @Pattern(regexp = REGEX_ULID)`, type `String`.
  - The page's `## 요청 파라미터` heading exists but is currently empty — `reportUlid` is undocumented.
- Relevant Code Excerpt:
  ```java
  @DeleteMapping(value = "/report/proposal-or-bug/{reportUlid}")
  public ResponseEntity<DataResponse<Void>> removeProposalOrBugReport(
          @PathVariable
          @NotBlank(message = "보고서 식별자가 비어 있습니다.")
          @Pattern(regexp = REGEX_ULID, message = "유효하지 않은 ULID 형식입니다. ")
          String reportUlid) { ... }
  ```

## kr.modusplant.domains.comment.framework.inbound.web.rest.CommentRestController

- Change Kind: Pre-existing gap (not a code change)
- Layer: RestController
- Domain/Package: domains.comment
- Facts:
  - `gatherByPost` (`GET /api/v1/communication/comments/post/{ulid}`): path variable bound to method parameter `postUlid` — `@PathVariable(required = false, value = "ulid") @NotBlank`, type `String` (ULID). The method has no query parameters (`page`/`size` belong to the unrelated `gatherByAuthor` method, not this one).
  - The page currently has no `## 요청 파라미터` section at all — `postUlid` is entirely undocumented.
- Relevant Code Excerpt:
  ```java
  @GetMapping("/post/{ulid}")
  public ResponseEntity<DataResponse<List<CommentOfPostResponse>>> gatherByPost(
          @PathVariable(required = false, value = "ulid")
          @NotBlank(message = "게시글 식별자가 비어 있습니다.")
          String postUlid, ...) { ... }
  ```

## kr.modusplant.domains.account.identity.framework.inbound.web.rest.IdentityRestController

- Change Kind: Pre-existing gap (not a code change)
- Layer: RestController
- Domain/Package: domains.account.identity
- Facts:
  - `getAuthInfo` (`GET /api/v1/members/{id}/auth-info`): path variable `id` bound to method parameter `memberUuid` — `@PathVariable("id") @NotNull`, type `UUID`.
  - The page documents this endpoint's URL only as a code-block comment inside the request body section, not as a `### 경로 변수` table.
- Relevant Code Excerpt:
  ```java
  @GetMapping("/v1/members/{id}/auth-info")
  public ResponseEntity<DataResponse<IdentityAuthResponse>> getAuthInfo(
          @PathVariable("id")
          @NotNull(message = "사용자의 식별자 값이 비어 있습니다")
          UUID memberUuid, ...) { ... }
  ```

## kr.modusplant.domains.account.normal.framework.inbound.web.rest.NormalIdentityRestController

- Change Kind: Pre-existing gap (not a code change)
- Layer: RestController
- Domain/Package: domains.account.normal
- Facts:
  - `modifyEmail` (`POST /api/v1/members/{id}/modify/email`): path variable `id` bound to method parameter `memberUuid` — `@PathVariable("id") @NotNull`, type `UUID`.
  - `modifyPassword` (`POST /api/v1/members/{id}/modify/password`): path variable `id` bound to method parameter `memberUuid` — `@PathVariable("id") @NotNull`, type `UUID`.
  - Both pages document this endpoint's URL only as a code-block comment inside the request body section, not as a `### 경로 변수` table.
- Relevant Code Excerpt:
  ```java
  @PostMapping("/api/v1/members/{id}/modify/email")
  public ResponseEntity<DataResponse<Void>> modifyEmail(
          @PathVariable("id")
          @NotNull(message = "사용자의 식별자 값이 비어 있습니다")
          UUID memberUuid, ...) { ... }
  ```
